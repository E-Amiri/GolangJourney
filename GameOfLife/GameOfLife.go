package main
 import (
	"fmt"
	"math/rand"
	"time")

var width, height int = 0, 0
fmt.Println("The Width of your canvas is ")
fmt.Scanln(&width)
fmt.Println("The Height of your canvas is ")
fmt.Scanln(&height)

func main() {
	for x = 0; x < width; x++ {
		column = []
		for y = 0; y < height; y++{
			if rand
		}
	}
}
